spring:
  application:
    name: hasher

  cloud:
    config:
      uri: http://localhost:40001/config-server

  profiles:
    active: sit

server:
  servlet:
    context-path: /hasher
    
app-config:
  shaAlgo: SHA-1
